import { LedFilterPipe } from './led-filter.pipe';

describe('LedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
